import React from "react";
import { storiesOf } from "@storybook/react";
import MenuBar from "./MenuBar";
import Unwrapper from "../Unwrapper/Unwrapper";

const MenuBarStory = <MenuBar />;
storiesOf("MenuBar").add("default", () => (
    <div>
        <Unwrapper componentName={"MenuBar"} storyName={"default"} />
        {MenuBarStory}
    </div>
));

export default MenuBarStory;
